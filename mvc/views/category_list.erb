<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 100%;
}

td, th {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: #dddddd;
}
</style>
</head>
<body>

<table>
    <tr>
        <th>Id</th>
        <th>Category</th>
        <th></th>
    </tr>
    <% categories.each do |category| %>
    <form>
    <tr>
        <input id="id" name= "id" type="hidden" value = <%= category.id%>>
        <td><%= category.id %></td>

        <input id="name" name= "name" type="hidden" value = <%= category.name%>>
        <td><%= category.name %></td>

        <td>
        <button type="submit" class="delete" formaction="/categories/show" formmethod="post">Show</button>
        <button type="submit" class="delete" formaction="/categories/delete" formmethod="post">Delete</button>
        <button type="submit" class="update" formaction="/categories/show" formmethod="post">Update</button>
        </td>
    </tr>
    </form>
    <% end %>
</table>

<a href='/categories/add' method='get'>Add </a>
</body>
</html>

</body>
</html>
